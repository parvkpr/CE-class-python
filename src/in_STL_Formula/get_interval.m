function [interval] = get_interval(phi)
 % GET_INTERVAL(PHI) returns the information about the time interval
 % associated witht the STL formula (PHI).
 %
 % Usage Example:
 %
 % interval = get_interval(myformula)
 %
 interval = phi.interval;
end